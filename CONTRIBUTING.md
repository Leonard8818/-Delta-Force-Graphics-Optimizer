# 参与贡献

欢迎提 Issue 和 PR。这个项目改的是别人电脑的系统设置，所以对"能否安全还原"看得比"多加一个功能"重。

本仓库的贡献范围仅限 Windows 客户端及其构建、测试代码；官网、数据接收服务和运营看板不在公开仓库中维护。

## 提 Bug

请附上：

1. **运行日志**——界面上「运行日志」页有一键复制按钮，或点「上传诊断报告」拿到取件码一并给出
2. 系统与硬件（日志开头有）
3. 哪一项没生效 / 报了什么错，最好带上错误原文

某项优化执行后仍显示「待优化」时，用管理员 PowerShell 跑一次诊断脚本并附上完整输出：

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\diagnose.ps1
```

## 新增优化项的要求

**能还原是硬门槛。** 一项优化如果无法可靠地回到改动前的状态，就不要提交——宁可少一项功能。具体要求：

- 改动前把原值完整记录进备份（包括"原本不存在"这种状态，还原时应删除而不是写默认值）
- 接入现有的 `Invoke-ApplyOp` / `Invoke-Restore` 机制，不要绕过备份自己写注册表
- 写入后回读校验，失败要如实报错并带上原始错误信息，**不允许静默当作成功**
- 在项的 `Note` 里如实写明副作用；有争议或已知反例的项默认不勾选，并归入 `risky` 档
- 收益不确定就别把话说满——"收益微弱"比"立竿见影"更可取

## 代码约定

- PowerShell 5.1 兼容（不能只在 PS7 下能跑）
- `.ps1` 存为 **UTF-8 带 BOM**，否则中文在 5.1 下乱码
- 注释解释"为什么"，不解释"是什么"；简洁为上
- 界面文案中文，避免黑话

### 几个踩过的坑

- GUI 点源引擎后共享作用域，**变量名不要与引擎的 param 同名**（`Items`/`GamePath`/`Preset`/`Progress` 等），否则强类型参数会把对象转成字符串
- 方法括号内的逗号会被当成方法参数：`AppendText("...{1}" -f (Get-Date), $Msg)` 会抛异常，要先算好整行
- 原生命令（powercfg/bcdedit/schtasks）找不到目标时往 stderr 写字，在 `$ErrorActionPreference='Stop'` 下会变成终止错误，需局部降级
- 对话框是独立 Window，不继承主窗口资源，样式要挂共享资源字典
- 电源方案注册表键只有 SYSTEM 可写，管理员也只读——写要走 `powercfg`，直删子键会被拒

## 验证

改了界面就跑一次离屏渲染确认（不要靠语法检查过关就提交，语法检查不走事件处理器）；改了引擎就在真实系统上跑 `-Preview` 看检测结果。**不要在未经确认的情况下让别人执行 `-Apply`**。

## 许可

提交即表示同意你的贡献以本项目的 [MIT 许可](LICENSE) 发布。
