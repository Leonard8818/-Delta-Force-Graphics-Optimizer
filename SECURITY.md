# 安全说明与漏洞报告

本工具会修改 Windows 系统设置、具备联网更新与诊断上传能力，因此安全边界值得单独说明。

## 报告安全问题

发现安全问题请**不要公开提 Issue**，改用 GitHub 的私密报告通道
（仓库 Security 标签 → Report a vulnerability）。

请尽量说明：问题所在的文件与函数、复现步骤、影响范围。我会尽快回复并在修复后于发布说明中致谢（如你不希望署名请注明）。

## 本工具的安全设计

**改动可还原**：每一项可还原的系统设置改动先把原值以 `prepared` 写前日志持久化，再修改系统并标记 `applied`。备份位于受保护的 `%ProgramData%\DeltaForceBooster\backup`，采用严格 schema、目标白名单和 HMAC 完整性校验；纯检测项和明确标注不可还原的操作不写入该备份，备份只在本地使用，不上传。

**更新链路**：
- 更新清单与安装包仅从硬编码的官方域名经 HTTPS 获取，清单里的下载地址若指向白名单外的主机会被直接拒绝——**清单被篡改也无法把用户导向任意地址**
- 用户确认立即更新后，已通过验证的 `EngineHost.exe` 管理员会话把安装包下载到收紧 ACL 的受保护暂存目录，并校验 SHA256 与文件大小
- 安装器提权后根据参数和完整性 sidecar 再次校验安装包，拒绝目标路径链上的 reparse point
- 发布文件先完整解压到同卷 staging，核对内置清单后再事务切换；中途失败恢复旧版本
- 自动检查只做提醒，下载与安装必须由用户点击触发

**诊断上传**：需用户主动点击，上传前列出内容类别并请求确认；路径中的用户名等信息做脱敏处理；不上传备份文件的原始内容。

**自动调优 Beta**：候选组由客户端内置的版本化白名单定义，第一版只允许低风险、无需重启且具备完整备份的项目；本地规则负责样本有效性和胜负判断，不接受服务端或 AI 返回的任意命令、注册表路径或项目集合。每个候选保存自己的受保护备份，回滚时只消费该候选对应文件，失败即停止后续测试；显卡型号伪装等 risky 项不进入自动实验。

**权限**：普通权限启动器先校验安装布局、产品身份和关键文件 SHA256，再只对自有 `EngineHost.exe` 请求一次 UAC；主界面在这次软件会话中保持管理员权限，优化、还原、自动调优和更新不会重复请求。原登录用户目录中的迁移、缓存清理、浏览器与显卡面板启动由全生命周期低权限启动器通过固定白名单代理，提权界面不把任意命令交给低权限端。用户配置按 SID 保存在受保护的 `%ProgramData%` 区。程序默认安装到 Program Files；其他固定 NTFS 盘仅支持卷根一级永久受保护 anchor，代码位于其 `app` 子目录，启动和更新都会复验 anchor 的 ACL、High MIC、身份文件和 reparse 状态。工具不安装常驻服务、不注册开机启动（「锁定电源计划」项除外，它会创建带安装标识的唯一任务，还原时删除）。

## 已知局限（如实说明）

**SHA256 防不住服务器被攻破。** 更新清单与安装包托管在同一台服务器上，攻击者若控制了服务器，可以同时替换二者并配好哈希，校验会正常通过。真正能防这种情况的是代码签名（用私钥签名、客户端验证公钥），本项目**没有代码签名证书**，因此服务器的安全性就是更新链路的安全上限。

**没有代码签名的连带影响**：Windows SmartScreen 会提示「未知发布者」；因为要修改注册表与系统服务，部分杀毒软件可能误报。

**部分优化项有明确副作用**，工具内逐项标注并对争议项默认不勾选。详见 [DISCLAIMER.md](DISCLAIMER.md)。

## 支持范围

仅对最新发布版本提供修复。旧版本发现问题请先升级再确认。
